#include <vector>
#include <string>
using namespace std;
bool isBalanced(string expression) {
 vector<char> stack;
 
 for (char c : expression) {
  if (c == '(') {
   //Task: what do we do?
  }
  else if (c == ')') {
   // Task: what do we do?
  }
  }
  
 // Task: how do we know if balanced?
 return true;
}
